command:
  - node
  - bin/cli.js
  - --directory
  - .

tests:
  - name: valid
    args:
      - --configuration
      - test/rules/valid
    exit-status: 0
    stderr: |
      using configuration file: test/rules/valid
      traversing directory structure...
      checking file and directory rules...
      repository looks good
